# This is a simple example of GHA
name: say-hello-Spiced-actions
run-name: ${{ github.actor }} Hello Spiced!
on: [push]
jobs:
  hello-testbash:
    runs-on: ubuntu-22.04
    steps:
      - run: echo "Hello Spiced!"
